# AZOI 接口文档

基于当前代码状态整理，覆盖已实现/已暴露的 REST 接口。当前控制器已逐步采用 `ResponseEntity<Result<T>>` 结构，HTTP 状态码由控制层统一返回，业务层通过 `Result<T>` 表示业务成功/失败。

统一返回结构：

```json
{
  "obj": {},
  "code": 1,
  "msg": "ok"
}
```

字段说明：
- `obj`: 返回对象，成功时为业务数据，失败时通常为 `null`
- `code`: `1` 表示成功，`-1` 表示失败
- `msg`: 说明信息

基础地址：
- `http://localhost:8080`

说明：
- 项目使用 `Spring Boot 4.x` + `Java 17`
- 数据库 schema：`azoi`
- 目前接口返回常见状态码：`200 OK`、`201 Created`、`400 Bad Request`、`401 Unauthorized`、`404 Not Found`
- 目前代码里存在一些命名/签名不一致的地方，文档会标注出来

---

## 1. 通用说明

### 1.1 Result 通用返回结构

```java
public class Result<T> {
    public static final int SUCCESS = 1;
    public static final int FAIL = -1;

    T obj;
    int code;
    String msg;
}
```

### 1.2 典型成功返回

```json
{
  "obj": {
    "id": 1,
    "username": "Azathorpe"
  },
  "code": 1,
  "msg": "ok"
}
```

### 1.3 典型失败返回

```json
{
  "obj": null,
  "code": -1,
  "msg": "User not found"
}
```

---

## 2. 用户模块 /user

### 2.1 获取当前用户详情

- Method: `GET`
- URL: `/user/getUser`
- Query: `id` (Long)
- 说明: 根据用户 ID 获取当前用户详细信息，返回 `UserCurrentVO`
- 成功状态: `200 OK`
- 失败状态: `400 Bad Request`

请求示例：

```http
GET /user/getUser?id=1
```

返回示例：

```json
{
  "obj": {
    "username": "Azathorpe",
    "email": "13490876723@qq.com",
    "nickname": null,
    "avatarUrl": null,
    "role": 1,
    "rating": 0,
    "solvedCount": 0,
    "submitCount": 0,
    "banned": false,
    "banReason": null,
    "lastLoginAt": "2026-09-17T00:00:00Z",
    "lastLoginIp": "127.0.0.1",
    "createdAt": "2026-09-17T00:00:00Z",
    "updatedAt": "2026-09-17T00:00:00Z",
    "deletedAt": null
  },
  "code": 1,
  "msg": "ok"
}
```

---

### 2.2 获取用户公开信息

- Method: `GET`
- URL: `/user/{userId}`
- Path: `userId` (Long)
- 说明: 获取公开用户信息，返回 `UserInfoVO`
- 成功状态: `200 OK`
- 失败状态: `404 Not Found`

请求示例：

```http
GET /user/1
```

返回字段：
- `username`
- `nickname`
- `avatarUrl`
- `role`
- `rating`
- `solvedCount`
- `submitCount`
- `banned`
- `banReason`
- `lastLoginAt`
- `lastLoginIp`
- `createdAt`

---

### 2.3 获取用户角色

- Method: `GET`
- URL: `/user/getUserRole`
- Query: `userId` (Long)
- 说明: 查询某个用户的角色信息
- 真实源码中：`UserRoleService.getUserRoles()` 实际返回的是角色关联列表的语义，但接口/实现签名存在不一致，建议统一整理
- 成功状态: `200 OK`
- 失败状态: `400 Bad Request`

请求示例：

```http
GET /user/getUserRole?userId=1
```

---

### 2.4 获取某角色下的用户

- Method: `GET`
- URL: `/user/getRoleUser`
- Query: `roleId` (Long)
- 说明: 查询某个角色对应的用户列表，返回 `List<UserInfoVO>`
- 成功状态: `200 OK`
- 失败状态: `400 Bad Request`

请求示例：

```http
GET /user/getRoleUser?roleId=2
```

---

### 2.5 用户注册

- Method: `POST`
- URL: `/user/register`
- Body:

```json
{
  "username": "Azathorpe",
  "password": "12345678",
  "email": "13490876723@qq.com"
}
```

- 说明: 注册用户；会校验用户名和邮箱是否已存在，并对密码进行加密
- 成功状态: `201 Created`
- 失败状态: `400 Bad Request`

返回示例：

```json
{
  "obj": {
    "username": "Azathorpe",
    "nickname": null,
    "avatarUrl": null,
    "role": 1,
    "rating": 0,
    "solvedCount": 0,
    "submitCount": 0,
    "banned": false,
    "banReason": null,
    "lastLoginAt": null,
    "lastLoginIp": null,
    "createdAt": "2026-09-17T00:00:00Z"
  },
  "code": 1,
  "msg": "success"
}
```

---

### 2.6 用户登录

- Method: `POST`
- URL: `/user/login`
- Body:

```json
{
  "username": "Azathorpe",
  "password": "12345678"
}
```

- 说明: 校验用户名和密码；成功后更新 `lastLoginAt` 和 `lastLoginIp`
- 成功状态: `200 OK`
- 失败状态: `401 Unauthorized`

返回示例：

```json
{
  "obj": true,
  "code": 1,
  "msg": "User logged in"
}
```

失败示例：

```json
{
  "obj": false,
  "code": -1,
  "msg": "User not found or password incorrect"
}
```

---

### 2.7 删除用户

- Method: `DELETE`
- URL: `/user/delete`
- Query: `userId` (Long)
- 说明: 删除用户
- 成功状态: `200 OK`
- 失败状态: `400 Bad Request`

请求示例：

```http
DELETE /user/delete?userId=1
```

---

## 3. 批量用户模块 /users

### 3.1 批量注册用户

- Method: `POST`
- URL: `/users/register`
- Body:

```json
[
  {
    "username": "u1",
    "password": "123456",
    "email": "a@qq.com"
  },
  {
    "username": "u2",
    "password": "123456",
    "email": "b@qq.com"
  }
]
```

- 说明: 批量注册用户列表，返回 `List<UserInfoVO>`
- 成功状态: `201 Created`
- 失败状态: `400 Bad Request`

返回示例：

```json
{
  "obj": [
    {
      "username": "u1",
      "nickname": null,
      "avatarUrl": null,
      "role": 1,
      "rating": 0,
      "solvedCount": 0,
      "submitCount": 0,
      "banned": false,
      "banReason": null,
      "lastLoginAt": null,
      "lastLoginIp": null,
      "createdAt": "2026-09-17T00:00:00Z"
    },
    {
      "username": "u2",
      "nickname": null,
      "avatarUrl": null,
      "role": 1,
      "rating": 0,
      "solvedCount": 0,
      "submitCount": 0,
      "banned": false,
      "banReason": null,
      "lastLoginAt": null,
      "lastLoginIp": null,
      "createdAt": "2026-09-17T00:00:00Z"
    }
  ],
  "code": 1,
  "msg": "Batch user registration success"
}
```

---

## 4. 角色模块 /role

### 4.1 获取所有角色

- Method: `GET`
- URL: `/role/get`
- 说明: 获取所有角色列表
- 成功状态: `200 OK`
- 失败状态: `400 Bad Request`

返回示例：

```json
{
  "obj": [
    {
      "id": 1,
      "code": "ADMIN",
      "name": "管理员"
    },
    {
      "id": 2,
      "code": "USER",
      "name": "普通用户"
    }
  ],
  "code": 1,
  "msg": "ok"
}
```

---

### 4.2 新增角色

- Method: `POST`
- URL: `/role/add`
- Body:

```json
{
  "code": "ADMIN",
  "name": "管理员"
}
```

- 说明: 新增角色
- 成功状态: `201 Created`
- 失败状态: `400 Bad Request`

---

### 4.3 更新角色

- Method: `POST`
- URL: `/role/update`
- Body:

```json
{
  "id": 1,
  "code": "SUPER_ADMIN",
  "name": "超级管理员"
}
```

- 说明: 修改角色信息
- 成功状态: `200 OK`
- 失败状态: `400 Bad Request`

---

### 4.4 删除角色

- Method: `DELETE`
- URL: `/role/delete`
- Body:

```json
{
  "id": 1
}
```

- 说明: 删除角色，当前代码通过请求体中的 `Role.id` 取值
- 成功状态: `200 OK`
- 失败状态: `400 Bad Request`

---

## 5. 团队模块 /team

### 5.1 获取单个团队详情

- Method: `GET`
- URL: `/team/{teamId}`
- Path: `teamId` (Long)
- 说明: 获取团队详情，包括团队信息、创建者信息和成员列表
- 返回结构：`TeamVO`
- 成功状态: `200 OK`
- 失败状态: `404 Not Found`

返回示例：

```json
{
  "obj": {
    "name": "开发团队",
    "description": "项目协作团队",
    "type": 1,
    "createdAt": "2026-09-17T00:00:00Z",
    "creatorInfo": {
      "username": "Azathorpe",
      "nickname": null,
      "avatarUrl": null,
      "role": 1,
      "rating": 0,
      "solvedCount": 0,
      "submitCount": 0,
      "banned": false,
      "banReason": null,
      "lastLoginAt": null,
      "lastLoginIp": null,
      "createdAt": "2026-09-17T00:00:00Z"
    },
    "members": [
      {
        "username": "Azathorpe",
        "avatarUrl": null
      }
    ]
  },
  "code": 1,
  "msg": "ok"
}
```

---

### 5.2 获取所有团队

- Method: `GET`
- URL: `/team/getTeams`
- 说明: 获取所有团队列表，返回 `List<TeamVO>`
- 成功状态: `200 OK`
- 失败状态: `404 Not Found`

返回示例：

```json
{
  "obj": [
    {
      "name": "开发团队",
      "description": "项目协作团队",
      "type": 1,
      "createdAt": "2026-09-17T00:00:00Z",
      "creatorInfo": {
        "username": "Azathorpe",
        "nickname": null,
        "avatarUrl": null,
        "role": 1,
        "rating": 0,
        "solvedCount": 0,
        "submitCount": 0,
        "banned": false,
        "banReason": null,
        "lastLoginAt": null,
        "lastLoginIp": null,
        "createdAt": "2026-09-17T00:00:00Z"
      },
      "members": []
    }
  ],
  "code": 1,
  "msg": "ok"
}
```

---

### 5.3 加入团队

- Method: `POST`
- URL: `/team/joinTeam`
- Body:

```json
{
  "teamId": 2,
  "userId": 3
}
```

- 说明: 用户加入团队；当前业务逻辑已实现，并在 TeamMemberService 中检查是否已加入其他团队
- 成功状态: `201 Created`
- 失败状态: `400 Bad Request`

返回示例：

```json
{
  "obj": null,
  "code": 1,
  "msg": "Member added successfully"
}
```

---

### 5.4 退出团队

- Method: `POST`
- URL: `/team/quit`
- Body:

```json
{
  "teamId": 2,
  "userId": 3
}
```

- 说明: 用户退出当前团队
- 成功状态: `201 Created`
- 失败状态: `400 Bad Request`

返回示例：

```json
{
  "obj": null,
  "code": 1,
  "msg": "Member removed successfully"
}
```

---

### 5.5 创建团队

- Method: `POST`
- URL: `/team/create`
- Body:

```json
{
  "name": "开发团队",
  "description": "项目协作团队",
  "type": 1,
  "ownerId": 1
}
```

- 说明: 创建团队，创建成功后自动把创建者加入团队成员列表
- 成功状态: `201 Created`
- 失败状态: `400 Bad Request`

---

### 5.6 修改团队

- Method: `POST`
- URL: `/team/modify`
- Body:

```json
{
  "name": "更新后的团队",
  "description": "更新说明",
  "type": 2,
  "ownerId": 1
}
```

- 说明: 仅团队所有者可修改团队信息；代码中会校验 `ownerId`
- 成功状态: `200 OK`
- 失败状态: `400 Bad Request`

---

### 5.7 删除团队

- Method: `DELETE`
- URL: `/team/remove`
- Body:

```json
{
  "userId": 1,
  "teamId": 2
}
```

- 说明: 仅团队所有者允许删除，删除前会校验所有权；删除团队同时清理 `team_members` 相关数据
- 成功状态: `200 OK`
- 失败状态: `400 Bad Request`

---

## 6. 实体与 VO 结构

### 6.1 User

```json
{
  "id": 1,
  "username": "Azathorpe",
  "email": "example@qq.com",
  "passwordHash": "$2a$10$...",
  "nickname": "",
  "avatarUrl": "",
  "role": 1,
  "rating": 0,
  "solvedCount": 0,
  "submitCount": 0,
  "isBanned": false,
  "banReason": null,
  "lastLoginAt": "2026-09-17T00:00:00Z",
  "lastLoginIp": "127.0.0.1",
  "createdAt": "2026-09-17T00:00:00Z",
  "updatedAt": "2026-09-17T00:00:00Z",
  "deletedAt": null
}
```

---

### 6.2 Role

```json
{
  "id": 1,
  "code": "USER",
  "name": "普通用户"
}
```

---

### 6.3 Team

```json
{
  "id": 1,
  "name": "开发团队",
  "description": "项目协作团队",
  "ownerId": 1,
  "type": 1,
  "createdAt": "2026-09-17T00:00:00Z"
}
```

---

### 6.4 TeamMember

```json
{
  "id": {
    "teamId": 1,
    "userId": 2
  },
  "role": 1,
  "joinedAt": "2026-09-17T00:00:00Z"
}
```

---

### 6.5 UserInfoVO / UserCurrentVO

`UserInfoVO` 主要用于公开信息；`UserCurrentVO` 用于当前用户详情，字段更完整，包含 `email`、`createdAt`、`updatedAt`、`deletedAt` 等。

---

## 7. 当前已知问题 / 设计瑕疵

根据当前代码，以下问题仍需关注：

- `UserRoleService` 接口和实现存在签名不一致：`getUserRoles()` 的接口/实现返回类型语义不统一，建议统一为 `Result<List<UserRole>>` 或统一成 `Result<Role>` 的语义，并调整控制层类型。
- `UsersController` 目前只实现了批量注册，实际还没扩展为完整用户集合 CRUD。
- `/team/joinTeam` 和 `/team/quit` 目前是基于 `TeamUserIDDTO` 的直接请求体设计，仍可进一步抽离到更细的 DTO/命令对象。
- `delete` 接口当前直接物理删除/硬删除，未引入软删除设计。
- 权限校验、登录态、JWT、CSRF 等安全机制还未在控制层做统一封装。
- 业务层和控制层仍有一些命名不一致，如 `getUserRole`、`getRoleUser`、`getUsers` 等命名需要统一规范。

---

## 8. 建议

建议后续按以下方向完善：

1. 统一返回规范：业务层 `Result<T>` + 控制层 `ResponseEntity`，并统一错误码映射
2. 统一命名：`/user` 与 `/users` 分离职责，不混用同一语义
3. 统一鉴权：增加 JWT / Session / 权限拦截器
4. 统一数据模型：明确 `Role` 与 `UserRole` 的关系语义
5. 补全接口校验：参数校验、空值校验、重复注册校验、权限校验
6. 提供 Swagger/OpenAPI 文档，便于前后端联调

---

如果需要，我可以下一步直接把这个文档再整理成 Swagger/OpenAPI 3.0 的 YAML/JSON 版本。
