# AZOI 接口文档

基于当前 Spring Boot 项目代码生成，覆盖已实现的 REST API。接口返回统一使用 `Result<T>` 包装，JSON 结构如下：

```json
{
  "obj": {},
  "code": 1,
  "msg": "ok"
}
```

字段说明：
- `obj`: 返回对象数据，成功时通常为实际实体/VO，失败时为 `null`
- `code`: `1` 表示成功，`-1` 表示失败
- `msg`: 说明信息

基础地址：
- `http://localhost:8080`

说明：
- 项目使用 Spring Boot 4.x / Java 17
- 数据库 schema 为 `azoi`
- 控制器逐步迁移为使用 `ResponseEntity<Result<T>>` 风格，控制层返回 HTTP 状态码；部分接口仍未实现，见下方 TODO 列表

---

## 1. 用户模块 /user

### 1.1 获取当前用户详情

- Method: `GET`
- URL: `/user/getUser`
- Query: `id` (Long)
- 说明: 通过用户 ID 获取当前用户的详细资料（`UserCurrentVO`）
- 请求示例:

```http
GET /user/getUser?id=1
```

- 返回示例:

```json
{
  "obj": {
    "username": "Azathorpe",
    "email": "13490876723@qq.com",
    "nickname": null,
    "avatarUrl": null,
    "role": 1,
    "rating": 0,
    "solvedCount": 0,
    "submitCount": 0,
    "banned": false,
    "banReason": null,
    "lastLoginAt": "2026-09-17T00:00:00Z",
    "lastLoginIp": "127.0.0.1",
    "createdAt": "2026-09-17T00:00:00Z",
    "updatedAt": "2026-09-17T00:00:00Z",
    "deletedAt": null
  },
  "code": 1,
  "msg": "ok"
}
```

### 1.2 获取用户公开信息

- Method: `GET`
- URL: `/user/{userId}`
- Path: `userId` (Long)
- 说明: 获取用户公开信息，返回 `UserInfoVO`
- 请求示例:

```http
GET /user/1
```

- 返回字段：
  - `username`
  - `nickname`
  - `avatarUrl`
  - `role`
  - `rating`
  - `solvedCount`
  - `submitCount`
  - `banned`
  - `banReason`
  - `lastLoginAt`
  - `lastLoginIp`
  - `createdAt`

### 1.3 获取用户角色

- Method: `GET`
- URL: `/user/getUserRole`
- Query: `userId` (Long)
- 说明: 查询某个用户有哪些角色
- 返回：`Result<List<UserRole>>`
- 示例：

```http
GET /user/getUserRole?userId=1
```

### 1.4 获取某角色下所有用户

- Method: `GET`
- URL: `/user/getRoleUser`
- Query: `roleId` (Long)
- 说明: 查询指定角色下的用户列表，返回 `List<UserInfoVO>`
- 示例：

```http
GET /user/getRoleUser?roleId=2
```

### 1.5 用户注册

- Method: `POST`
- URL: `/user/register`
- Body:

```json
{
  "username": "Azathorpe",
  "password": "12345678",
  "email": "13490876723@qq.com"
}
```

- 说明: 注册用户时会校验 `username` 和 `email` 是否已存在；密码会使用 `PasswordEncoder` 进行加密
- 返回：`Result<UserInfoVO>`

### 1.6 用户登录

- Method: `POST`
- URL: `/user/login`
- Body:

```json
{
  "username": "Azathorpe",
  "password": "12345678"
}
```

- 说明: 通过用户名和密码匹配，成功后更新 `lastLoginAt` 和 `lastLoginIp`
- 返回：

```json
{
  "obj": null,
  "code": 1,
  "msg": "User logged in"
}
```

- 失败返回：

```json
{
  "obj": null,
  "code": -1,
  "msg": "User not found or password incorrect"
}
```

### 1.7 删除用户

- Method: `DELETE`
- URL: `/user/delete`
- Query: `userId` (Long)
- 说明: 根据用户 ID 删除用户
- 示例：

```http
DELETE /user/delete?userId=1
```

---

## 2. 角色模块 /role

### 2.1 获取所有角色

- Method: `GET`
- URL: `/role/get`
- 说明: 返回所有角色列表
- 返回示例：

```json
{
  "obj": [
    {
      "id": 1,
      "code": "ADMIN",
      "name": "管理员"
    },
    {
      "id": 2,
      "code": "USER",
      "name": "普通用户"
    }
  ],
  "code": 1,
  "msg": "ok"
}
```

### 2.2 新增角色

- Method: `POST`
- URL: `/role/add`
- Body:

```json
{
  "code": "ADMIN",
  "name": "管理员"
}
```

- 返回：新增后的 `Role` 对象

### 2.3 更新角色

- Method: `POST`
- URL: `/role/update`
- Body:

```json
{
  "id": 1,
  "code": "SUPER_ADMIN",
  "name": "超级管理员"
}
```

- 说明: 根据 `id` 更新角色的 `code` 和 `name`

### 2.4 删除角色

- Method: `DELETE`
- URL: `/role/delete`
- Body:

```json
{
  "id": 1
}
```

- 说明: 直接根据请求体中的 `Role` 对象取 `id` 删除

---

## 3. 团队模块 /team

### 3.1 获取团队详情

- Method: `GET`
- URL: `/team/{teamId}`
- Path: `teamId` (Long)
- 说明: 返回团队详情，包括团队信息、创建者信息、成员列表
- 返回结构：`TeamVO`
- 关键字段：
  - `name`
  - `description`
  - `type`
  - `createdAt`
  - `creatorInfo`
  - `members`

- 返回示例：

```json
{
  "obj": {
    "name": "开发团队",
    "description": "项目协作团队",
    "type": 1,
    "createdAt": "2026-09-17T00:00:00Z",
    "creatorInfo": {
      "username": "Azathorpe",
      "nickname": null,
      "avatarUrl": null,
      "role": 1,
      "rating": 0,
      "solvedCount": 0,
      "submitCount": 0,
      "banned": false,
      "banReason": null,
      "lastLoginAt": null,
      "lastLoginIp": null,
      "createdAt": "2026-09-17T00:00:00Z"
    },
    "members": [
      {
        "username": "Azathorpe",
        "avatarUrl": null
      }
    ]
  },
  "code": 1,
  "msg": "ok"
}
```

### 3.2 创建团队

- Method: `POST`
- URL: `/team/create`
- Body:

```json
{
  "name": "开发团队",
  "description": "项目协作团队",
  "type": 1,
  "ownerId": 1
}
```

- 说明: 创建团队时会自动把创建者加入 `team_members` 表，作为团队成员

### 3.3 修改团队

- Method: `POST`
- URL: `/team/modify`
- Body:

```json
{
  "name": "改名后的团队",
  "description": "更新说明",
  "type": 2,
  "ownerId": 1
}
```

- 说明: 根据 `ownerId` 验证该用户是否为团队所有者，确认后更新团队信息

### 3.4 删除团队

- Method: `DELETE`
- URL: `/team/remove`
- Body:

```json
{
  "userId": 1,
  "teamId": 2
}
```

- 说明: 仅团队所有者可删除，删除成功后返回 `Result<Team>` 其中 `obj` 为 `null`

### 3.5 加入团队

- Method: `POST`
- URL: `/team/joinTeam`
- Body:

```json
{
  "teamId": 2,
  "userId": 3
}
```

- 说明: 当前代码中仅返回字符串：`join team: (not impl)`，尚未实现

---

## 4. 通用数据结构

### 4.1 User

```json
{
  "id": 1,
  "username": "Azathorpe",
  "email": "example@qq.com",
  "passwordHash": "$2a$10$...",
  "nickname": "",
  "avatarUrl": "",
  "role": 1,
  "rating": 0,
  "solvedCount": 0,
  "submitCount": 0,
  "isBanned": false,
  "banReason": null,
  "lastLoginAt": "2026-09-17T00:00:00Z",
  "lastLoginIp": "127.0.0.1",
  "createdAt": "2026-09-17T00:00:00Z",
  "updatedAt": "2026-09-17T00:00:00Z",
  "deletedAt": null
}
```

### 4.2 Role

```json
{
  "id": 1,
  "code": "USER",
  "name": "普通用户"
}
```

### 4.3 Team

```json
{
  "id": 1,
  "name": "开发团队",
  "description": "项目协作团队",
  "ownerId": 1,
  "type": 1,
  "createdAt": "2026-09-17T00:00:00Z"
}
```

### 4.4 TeamMember

```json
{
  "id": {
    "teamId": 1,
    "userId": 2
  },
  "role": 1,
  "joinedAt": "2026-09-17T00:00:00Z"
}
```

---

## 5. 已知问题 / TODO（更新）

根据当前源码与最近迁移，以下接口或逻辑尚未完整实现或需进一步处理：

- 控制器迁移：UserController / RoleController / TeamController 已改为使用 `ResponseEntity<Result<T>>` 风格，控制层返回 HTTP 状态码（已编译通过）。
- `/users` 控制器为空，需补全用户集合接口（CRUD/分页/筛选等）。
- `/team/joinTeam` 返回占位字符串，尚未实现加入团队逻辑。
- `/user/resetPassword` 未实现（`Result.FAIL, "not impl"`）。
- `/user/removeNeverUsedUserRole` 未实现。
- 角色删除目前为硬删除；如需历史审计或恢复，建议改为软删除。
- `Result` 与 HTTP 状态码的映射需统一规范（已开始用 ResponseEntity，但仍需定义错误码策略）。
- 权限和鉴权：登录态、JWT、权限校验、CSRF 等安全机制尚未实现或未集成到控制器。

---

---

## 6. 建议

建议后续按以下方向完善：

1. 统一接口返回标准：HTTP status + JSON body
2. 增加统一鉴权（JWT 或 Session）
3. 规范命名：例如 `/users` 统一作为用户集合接口，保留 `/user/{id}` 等单例接口
4. 对未实现接口补齐或移除
5. 增加接口测试与请求参数校验

如果需要，我可以下一步直接把这份文档整理成更适合前后端联调的 Swagger/OpenAPI 格式。
